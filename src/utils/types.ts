export type LoginResponse = {
  full_name: string;
  home_page: string;
  message: string;
};

export type Cookies = {
  sid: string;
  system_user: string;
  full_name: string;
  user_id: string;
  user_image: string;
};
